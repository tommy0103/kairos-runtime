// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               v3.19.1
// source: vfs.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "kairos.vfs.v1";

export enum SearchMode {
  SEARCH_MODE_UNSPECIFIED = 0,
  SEARCH_MODE_SEMANTIC = 1,
  SEARCH_MODE_EXACT = 2,
  UNRECOGNIZED = -1,
}

export function searchModeFromJSON(object: any): SearchMode {
  switch (object) {
    case 0:
    case "SEARCH_MODE_UNSPECIFIED":
      return SearchMode.SEARCH_MODE_UNSPECIFIED;
    case 1:
    case "SEARCH_MODE_SEMANTIC":
      return SearchMode.SEARCH_MODE_SEMANTIC;
    case 2:
    case "SEARCH_MODE_EXACT":
      return SearchMode.SEARCH_MODE_EXACT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SearchMode.UNRECOGNIZED;
  }
}

export function searchModeToJSON(object: SearchMode): string {
  switch (object) {
    case SearchMode.SEARCH_MODE_UNSPECIFIED:
      return "SEARCH_MODE_UNSPECIFIED";
    case SearchMode.SEARCH_MODE_SEMANTIC:
      return "SEARCH_MODE_SEMANTIC";
    case SearchMode.SEARCH_MODE_EXACT:
      return "SEARCH_MODE_EXACT";
    case SearchMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ReadRequest {
  path: string;
}

export interface ReadResponse {
  success: boolean;
  content: string;
  errorMsg: string;
}

export interface WriteRequest {
  path: string;
  content: string;
}

export interface WriteResponse {
  success: boolean;
  errorMsg: string;
}

export interface PatchRequest {
  path: string;
  partialContent: string;
}

export interface PatchResponse {
  success: boolean;
  errorMsg: string;
}

export interface SearchRequest {
  query: string;
  scope: string;
  limit: number;
  mode: SearchMode;
}

export interface SearchResponse {
  results: SearchResult[];
}

export interface SearchResult {
  sessionId: string;
  centerVector: number[];
  abstractSummary: string;
  messages: ChatMessage[];
  score: number;
}

export interface ArchiveRequest {
  sessionId: string;
  chatId: string;
  centroidVector: number[];
  abstractSummary: string;
  messages: ChatMessage[];
}

export interface ArchiveResponse {
  success: boolean;
  errorMsg: string;
}

export interface ChatMessage {
  userId: string;
  messageId: string;
  chatId: string;
  conversationType: string;
  context: string;
  timestamp: number;
  metadata: MessageMetadata | undefined;
  vector: number[];
}

export interface MessageMetadata {
  isBot: boolean;
  username: string;
  replyToMessageId: string;
  replyToUserId: string;
  isReplyToMe: boolean;
  isMentionMe: boolean;
  mentions: string[];
}

function createBaseReadRequest(): ReadRequest {
  return { path: "" };
}

export const ReadRequest: MessageFns<ReadRequest> = {
  encode(message: ReadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadRequest {
    return { path: isSet(object.path) ? globalThis.String(object.path) : "" };
  },

  toJSON(message: ReadRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create(base?: DeepPartial<ReadRequest>): ReadRequest {
    return ReadRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReadRequest>): ReadRequest {
    const message = createBaseReadRequest();
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseReadResponse(): ReadResponse {
  return { success: false, content: "", errorMsg: "" };
}

export const ReadResponse: MessageFns<ReadResponse> = {
  encode(message: ReadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    if (message.errorMsg !== "") {
      writer.uint32(26).string(message.errorMsg);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorMsg = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      errorMsg: isSet(object.errorMsg)
        ? globalThis.String(object.errorMsg)
        : isSet(object.error_msg)
        ? globalThis.String(object.error_msg)
        : "",
    };
  },

  toJSON(message: ReadResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.errorMsg !== "") {
      obj.errorMsg = message.errorMsg;
    }
    return obj;
  },

  create(base?: DeepPartial<ReadResponse>): ReadResponse {
    return ReadResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReadResponse>): ReadResponse {
    const message = createBaseReadResponse();
    message.success = object.success ?? false;
    message.content = object.content ?? "";
    message.errorMsg = object.errorMsg ?? "";
    return message;
  },
};

function createBaseWriteRequest(): WriteRequest {
  return { path: "", content: "" };
}

export const WriteRequest: MessageFns<WriteRequest> = {
  encode(message: WriteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WriteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWriteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WriteRequest {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: WriteRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<WriteRequest>): WriteRequest {
    return WriteRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WriteRequest>): WriteRequest {
    const message = createBaseWriteRequest();
    message.path = object.path ?? "";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseWriteResponse(): WriteResponse {
  return { success: false, errorMsg: "" };
}

export const WriteResponse: MessageFns<WriteResponse> = {
  encode(message: WriteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.errorMsg !== "") {
      writer.uint32(18).string(message.errorMsg);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WriteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWriteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.errorMsg = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WriteResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      errorMsg: isSet(object.errorMsg)
        ? globalThis.String(object.errorMsg)
        : isSet(object.error_msg)
        ? globalThis.String(object.error_msg)
        : "",
    };
  },

  toJSON(message: WriteResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.errorMsg !== "") {
      obj.errorMsg = message.errorMsg;
    }
    return obj;
  },

  create(base?: DeepPartial<WriteResponse>): WriteResponse {
    return WriteResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WriteResponse>): WriteResponse {
    const message = createBaseWriteResponse();
    message.success = object.success ?? false;
    message.errorMsg = object.errorMsg ?? "";
    return message;
  },
};

function createBasePatchRequest(): PatchRequest {
  return { path: "", partialContent: "" };
}

export const PatchRequest: MessageFns<PatchRequest> = {
  encode(message: PatchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.partialContent !== "") {
      writer.uint32(18).string(message.partialContent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PatchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePatchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.partialContent = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PatchRequest {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      partialContent: isSet(object.partialContent)
        ? globalThis.String(object.partialContent)
        : isSet(object.partial_content)
        ? globalThis.String(object.partial_content)
        : "",
    };
  },

  toJSON(message: PatchRequest): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.partialContent !== "") {
      obj.partialContent = message.partialContent;
    }
    return obj;
  },

  create(base?: DeepPartial<PatchRequest>): PatchRequest {
    return PatchRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PatchRequest>): PatchRequest {
    const message = createBasePatchRequest();
    message.path = object.path ?? "";
    message.partialContent = object.partialContent ?? "";
    return message;
  },
};

function createBasePatchResponse(): PatchResponse {
  return { success: false, errorMsg: "" };
}

export const PatchResponse: MessageFns<PatchResponse> = {
  encode(message: PatchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.errorMsg !== "") {
      writer.uint32(18).string(message.errorMsg);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PatchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePatchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.errorMsg = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PatchResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      errorMsg: isSet(object.errorMsg)
        ? globalThis.String(object.errorMsg)
        : isSet(object.error_msg)
        ? globalThis.String(object.error_msg)
        : "",
    };
  },

  toJSON(message: PatchResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.errorMsg !== "") {
      obj.errorMsg = message.errorMsg;
    }
    return obj;
  },

  create(base?: DeepPartial<PatchResponse>): PatchResponse {
    return PatchResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PatchResponse>): PatchResponse {
    const message = createBasePatchResponse();
    message.success = object.success ?? false;
    message.errorMsg = object.errorMsg ?? "";
    return message;
  },
};

function createBaseSearchRequest(): SearchRequest {
  return { query: "", scope: "", limit: 0, mode: 0 };
}

export const SearchRequest: MessageFns<SearchRequest> = {
  encode(message: SearchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    if (message.scope !== "") {
      writer.uint32(18).string(message.scope);
    }
    if (message.limit !== 0) {
      writer.uint32(24).int32(message.limit);
    }
    if (message.mode !== 0) {
      writer.uint32(32).int32(message.mode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.scope = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.mode = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      scope: isSet(object.scope) ? globalThis.String(object.scope) : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      mode: isSet(object.mode) ? searchModeFromJSON(object.mode) : 0,
    };
  },

  toJSON(message: SearchRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.scope !== "") {
      obj.scope = message.scope;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.mode !== 0) {
      obj.mode = searchModeToJSON(message.mode);
    }
    return obj;
  },

  create(base?: DeepPartial<SearchRequest>): SearchRequest {
    return SearchRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchRequest>): SearchRequest {
    const message = createBaseSearchRequest();
    message.query = object.query ?? "";
    message.scope = object.scope ?? "";
    message.limit = object.limit ?? 0;
    message.mode = object.mode ?? 0;
    return message;
  },
};

function createBaseSearchResponse(): SearchResponse {
  return { results: [] };
}

export const SearchResponse: MessageFns<SearchResponse> = {
  encode(message: SearchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      SearchResult.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(SearchResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchResponse {
    return {
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => SearchResult.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SearchResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => SearchResult.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<SearchResponse>): SearchResponse {
    return SearchResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchResponse>): SearchResponse {
    const message = createBaseSearchResponse();
    message.results = object.results?.map((e) => SearchResult.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSearchResult(): SearchResult {
  return { sessionId: "", centerVector: [], abstractSummary: "", messages: [], score: 0 };
}

export const SearchResult: MessageFns<SearchResult> = {
  encode(message: SearchResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    writer.uint32(18).fork();
    for (const v of message.centerVector) {
      writer.float(v);
    }
    writer.join();
    if (message.abstractSummary !== "") {
      writer.uint32(26).string(message.abstractSummary);
    }
    for (const v of message.messages) {
      ChatMessage.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.score !== 0) {
      writer.uint32(45).float(message.score);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag === 21) {
            message.centerVector.push(reader.float());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.centerVector.push(reader.float());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.abstractSummary = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.messages.push(ChatMessage.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.score = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchResult {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      centerVector: globalThis.Array.isArray(object?.centerVector)
        ? object.centerVector.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.center_vector)
        ? object.center_vector.map((e: any) => globalThis.Number(e))
        : [],
      abstractSummary: isSet(object.abstractSummary)
        ? globalThis.String(object.abstractSummary)
        : isSet(object.abstract_summary)
        ? globalThis.String(object.abstract_summary)
        : "",
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => ChatMessage.fromJSON(e))
        : [],
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
    };
  },

  toJSON(message: SearchResult): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.centerVector?.length) {
      obj.centerVector = message.centerVector;
    }
    if (message.abstractSummary !== "") {
      obj.abstractSummary = message.abstractSummary;
    }
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => ChatMessage.toJSON(e));
    }
    if (message.score !== 0) {
      obj.score = message.score;
    }
    return obj;
  },

  create(base?: DeepPartial<SearchResult>): SearchResult {
    return SearchResult.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchResult>): SearchResult {
    const message = createBaseSearchResult();
    message.sessionId = object.sessionId ?? "";
    message.centerVector = object.centerVector?.map((e) => e) || [];
    message.abstractSummary = object.abstractSummary ?? "";
    message.messages = object.messages?.map((e) => ChatMessage.fromPartial(e)) || [];
    message.score = object.score ?? 0;
    return message;
  },
};

function createBaseArchiveRequest(): ArchiveRequest {
  return { sessionId: "", chatId: "", centroidVector: [], abstractSummary: "", messages: [] };
}

export const ArchiveRequest: MessageFns<ArchiveRequest> = {
  encode(message: ArchiveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.chatId !== "") {
      writer.uint32(18).string(message.chatId);
    }
    writer.uint32(26).fork();
    for (const v of message.centroidVector) {
      writer.float(v);
    }
    writer.join();
    if (message.abstractSummary !== "") {
      writer.uint32(34).string(message.abstractSummary);
    }
    for (const v of message.messages) {
      ChatMessage.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chatId = reader.string();
          continue;
        }
        case 3: {
          if (tag === 29) {
            message.centroidVector.push(reader.float());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.centroidVector.push(reader.float());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.abstractSummary = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.messages.push(ChatMessage.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArchiveRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      chatId: isSet(object.chatId)
        ? globalThis.String(object.chatId)
        : isSet(object.chat_id)
        ? globalThis.String(object.chat_id)
        : "",
      centroidVector: globalThis.Array.isArray(object?.centroidVector)
        ? object.centroidVector.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.centroid_vector)
        ? object.centroid_vector.map((e: any) => globalThis.Number(e))
        : [],
      abstractSummary: isSet(object.abstractSummary)
        ? globalThis.String(object.abstractSummary)
        : isSet(object.abstract_summary)
        ? globalThis.String(object.abstract_summary)
        : "",
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => ChatMessage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ArchiveRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.chatId !== "") {
      obj.chatId = message.chatId;
    }
    if (message.centroidVector?.length) {
      obj.centroidVector = message.centroidVector;
    }
    if (message.abstractSummary !== "") {
      obj.abstractSummary = message.abstractSummary;
    }
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => ChatMessage.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ArchiveRequest>): ArchiveRequest {
    return ArchiveRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ArchiveRequest>): ArchiveRequest {
    const message = createBaseArchiveRequest();
    message.sessionId = object.sessionId ?? "";
    message.chatId = object.chatId ?? "";
    message.centroidVector = object.centroidVector?.map((e) => e) || [];
    message.abstractSummary = object.abstractSummary ?? "";
    message.messages = object.messages?.map((e) => ChatMessage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseArchiveResponse(): ArchiveResponse {
  return { success: false, errorMsg: "" };
}

export const ArchiveResponse: MessageFns<ArchiveResponse> = {
  encode(message: ArchiveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.errorMsg !== "") {
      writer.uint32(18).string(message.errorMsg);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.errorMsg = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArchiveResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      errorMsg: isSet(object.errorMsg)
        ? globalThis.String(object.errorMsg)
        : isSet(object.error_msg)
        ? globalThis.String(object.error_msg)
        : "",
    };
  },

  toJSON(message: ArchiveResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.errorMsg !== "") {
      obj.errorMsg = message.errorMsg;
    }
    return obj;
  },

  create(base?: DeepPartial<ArchiveResponse>): ArchiveResponse {
    return ArchiveResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ArchiveResponse>): ArchiveResponse {
    const message = createBaseArchiveResponse();
    message.success = object.success ?? false;
    message.errorMsg = object.errorMsg ?? "";
    return message;
  },
};

function createBaseChatMessage(): ChatMessage {
  return {
    userId: "",
    messageId: "",
    chatId: "",
    conversationType: "",
    context: "",
    timestamp: 0,
    metadata: undefined,
    vector: [],
  };
}

export const ChatMessage: MessageFns<ChatMessage> = {
  encode(message: ChatMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.messageId !== "") {
      writer.uint32(18).string(message.messageId);
    }
    if (message.chatId !== "") {
      writer.uint32(26).string(message.chatId);
    }
    if (message.conversationType !== "") {
      writer.uint32(34).string(message.conversationType);
    }
    if (message.context !== "") {
      writer.uint32(42).string(message.context);
    }
    if (message.timestamp !== 0) {
      writer.uint32(48).int64(message.timestamp);
    }
    if (message.metadata !== undefined) {
      MessageMetadata.encode(message.metadata, writer.uint32(58).fork()).join();
    }
    writer.uint32(66).fork();
    for (const v of message.vector) {
      writer.float(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChatMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChatMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.messageId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.chatId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.conversationType = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.context = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.metadata = MessageMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag === 69) {
            message.vector.push(reader.float());

            continue;
          }

          if (tag === 66) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.vector.push(reader.float());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChatMessage {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      messageId: isSet(object.messageId)
        ? globalThis.String(object.messageId)
        : isSet(object.message_id)
        ? globalThis.String(object.message_id)
        : "",
      chatId: isSet(object.chatId)
        ? globalThis.String(object.chatId)
        : isSet(object.chat_id)
        ? globalThis.String(object.chat_id)
        : "",
      conversationType: isSet(object.conversationType)
        ? globalThis.String(object.conversationType)
        : isSet(object.conversation_type)
        ? globalThis.String(object.conversation_type)
        : "",
      context: isSet(object.context) ? globalThis.String(object.context) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      metadata: isSet(object.metadata) ? MessageMetadata.fromJSON(object.metadata) : undefined,
      vector: globalThis.Array.isArray(object?.vector)
        ? object.vector.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: ChatMessage): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.messageId !== "") {
      obj.messageId = message.messageId;
    }
    if (message.chatId !== "") {
      obj.chatId = message.chatId;
    }
    if (message.conversationType !== "") {
      obj.conversationType = message.conversationType;
    }
    if (message.context !== "") {
      obj.context = message.context;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.metadata !== undefined) {
      obj.metadata = MessageMetadata.toJSON(message.metadata);
    }
    if (message.vector?.length) {
      obj.vector = message.vector;
    }
    return obj;
  },

  create(base?: DeepPartial<ChatMessage>): ChatMessage {
    return ChatMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ChatMessage>): ChatMessage {
    const message = createBaseChatMessage();
    message.userId = object.userId ?? "";
    message.messageId = object.messageId ?? "";
    message.chatId = object.chatId ?? "";
    message.conversationType = object.conversationType ?? "";
    message.context = object.context ?? "";
    message.timestamp = object.timestamp ?? 0;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? MessageMetadata.fromPartial(object.metadata)
      : undefined;
    message.vector = object.vector?.map((e) => e) || [];
    return message;
  },
};

function createBaseMessageMetadata(): MessageMetadata {
  return {
    isBot: false,
    username: "",
    replyToMessageId: "",
    replyToUserId: "",
    isReplyToMe: false,
    isMentionMe: false,
    mentions: [],
  };
}

export const MessageMetadata: MessageFns<MessageMetadata> = {
  encode(message: MessageMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isBot !== false) {
      writer.uint32(8).bool(message.isBot);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.replyToMessageId !== "") {
      writer.uint32(26).string(message.replyToMessageId);
    }
    if (message.replyToUserId !== "") {
      writer.uint32(34).string(message.replyToUserId);
    }
    if (message.isReplyToMe !== false) {
      writer.uint32(40).bool(message.isReplyToMe);
    }
    if (message.isMentionMe !== false) {
      writer.uint32(48).bool(message.isMentionMe);
    }
    for (const v of message.mentions) {
      writer.uint32(58).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isBot = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.replyToMessageId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.replyToUserId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isReplyToMe = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.isMentionMe = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.mentions.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageMetadata {
    return {
      isBot: isSet(object.isBot)
        ? globalThis.Boolean(object.isBot)
        : isSet(object.is_bot)
        ? globalThis.Boolean(object.is_bot)
        : false,
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      replyToMessageId: isSet(object.replyToMessageId)
        ? globalThis.String(object.replyToMessageId)
        : isSet(object.reply_to_message_id)
        ? globalThis.String(object.reply_to_message_id)
        : "",
      replyToUserId: isSet(object.replyToUserId)
        ? globalThis.String(object.replyToUserId)
        : isSet(object.reply_to_user_id)
        ? globalThis.String(object.reply_to_user_id)
        : "",
      isReplyToMe: isSet(object.isReplyToMe)
        ? globalThis.Boolean(object.isReplyToMe)
        : isSet(object.is_reply_to_me)
        ? globalThis.Boolean(object.is_reply_to_me)
        : false,
      isMentionMe: isSet(object.isMentionMe)
        ? globalThis.Boolean(object.isMentionMe)
        : isSet(object.is_mention_me)
        ? globalThis.Boolean(object.is_mention_me)
        : false,
      mentions: globalThis.Array.isArray(object?.mentions)
        ? object.mentions.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: MessageMetadata): unknown {
    const obj: any = {};
    if (message.isBot !== false) {
      obj.isBot = message.isBot;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.replyToMessageId !== "") {
      obj.replyToMessageId = message.replyToMessageId;
    }
    if (message.replyToUserId !== "") {
      obj.replyToUserId = message.replyToUserId;
    }
    if (message.isReplyToMe !== false) {
      obj.isReplyToMe = message.isReplyToMe;
    }
    if (message.isMentionMe !== false) {
      obj.isMentionMe = message.isMentionMe;
    }
    if (message.mentions?.length) {
      obj.mentions = message.mentions;
    }
    return obj;
  },

  create(base?: DeepPartial<MessageMetadata>): MessageMetadata {
    return MessageMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MessageMetadata>): MessageMetadata {
    const message = createBaseMessageMetadata();
    message.isBot = object.isBot ?? false;
    message.username = object.username ?? "";
    message.replyToMessageId = object.replyToMessageId ?? "";
    message.replyToUserId = object.replyToUserId ?? "";
    message.isReplyToMe = object.isReplyToMe ?? false;
    message.isMentionMe = object.isMentionMe ?? false;
    message.mentions = object.mentions?.map((e) => e) || [];
    return message;
  },
};

export type MemoryVFSDefinition = typeof MemoryVFSDefinition;
export const MemoryVFSDefinition = {
  name: "MemoryVFS",
  fullName: "kairos.vfs.v1.MemoryVFS",
  methods: {
    read: {
      name: "Read",
      requestType: ReadRequest,
      requestStream: false,
      responseType: ReadResponse,
      responseStream: false,
      options: {},
    },
    write: {
      name: "Write",
      requestType: WriteRequest,
      requestStream: false,
      responseType: WriteResponse,
      responseStream: false,
      options: {},
    },
    patch: {
      name: "Patch",
      requestType: PatchRequest,
      requestStream: false,
      responseType: PatchResponse,
      responseStream: false,
      options: {},
    },
    search: {
      name: "Search",
      requestType: SearchRequest,
      requestStream: false,
      responseType: SearchResponse,
      responseStream: false,
      options: {},
    },
    archive: {
      name: "Archive",
      requestType: ArchiveRequest,
      requestStream: false,
      responseType: ArchiveResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface MemoryVFSServiceImplementation<CallContextExt = {}> {
  read(request: ReadRequest, context: CallContext & CallContextExt): Promise<DeepPartial<ReadResponse>>;
  write(request: WriteRequest, context: CallContext & CallContextExt): Promise<DeepPartial<WriteResponse>>;
  patch(request: PatchRequest, context: CallContext & CallContextExt): Promise<DeepPartial<PatchResponse>>;
  search(request: SearchRequest, context: CallContext & CallContextExt): Promise<DeepPartial<SearchResponse>>;
  archive(request: ArchiveRequest, context: CallContext & CallContextExt): Promise<DeepPartial<ArchiveResponse>>;
}

export interface MemoryVFSClient<CallOptionsExt = {}> {
  read(request: DeepPartial<ReadRequest>, options?: CallOptions & CallOptionsExt): Promise<ReadResponse>;
  write(request: DeepPartial<WriteRequest>, options?: CallOptions & CallOptionsExt): Promise<WriteResponse>;
  patch(request: DeepPartial<PatchRequest>, options?: CallOptions & CallOptionsExt): Promise<PatchResponse>;
  search(request: DeepPartial<SearchRequest>, options?: CallOptions & CallOptionsExt): Promise<SearchResponse>;
  archive(request: DeepPartial<ArchiveRequest>, options?: CallOptions & CallOptionsExt): Promise<ArchiveResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
