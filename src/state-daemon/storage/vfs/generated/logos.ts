// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               v6.33.4
// source: logos.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "logos.kernel.v1";

export interface ReadReq {
  uri: string;
}

export interface ReadRes {
  content: string;
}

export interface WriteReq {
  uri: string;
  content: string;
}

export interface WriteRes {
}

export interface PatchReq {
  uri: string;
  partial: string;
}

export interface PatchRes {
}

export interface ExecReq {
  command: string;
}

export interface ExecRes {
  stdout: string;
  stderr: string;
  exitCode: number;
}

export interface CallReq {
  tool: string;
  paramsJson: string;
}

export interface CallRes {
  resultJson: string;
}

export interface CompleteReq {
  /** required: what happened this turn */
  summary: string;
  /** optional: message to deliver to the user */
  reply: string;
  /** optional: create a task completion anchor? */
  anchor: boolean;
  /** optional: execution details to append to log */
  taskLog: string;
  /** optional: "recoverable_error" | "awaiting_user" */
  sleepReason: string;
  /** optional: hint to adapter — auto-retry? */
  sleepRetry: boolean;
  /** optional: abandon current task, resume this one */
  resumeTaskId: string;
  /** optional: JSON array of fact objects for the anchor */
  anchorFacts: string;
}

export interface CompleteRes {
  /** echo back for adapter delivery */
  reply: string;
  /** created anchor ID, empty if anchor was false */
  anchorId: string;
}

export interface HandshakeReq {
  token: string;
}

export interface HandshakeRes {
  ok: boolean;
  error: string;
}

export interface RegisterTokenReq {
  token: string;
  taskId: string;
  role: string;
}

export interface RegisterTokenRes {
}

export interface RevokeTokenReq {
  token: string;
}

export interface RevokeTokenRes {
}

function createBaseReadReq(): ReadReq {
  return { uri: "" };
}

export const ReadReq: MessageFns<ReadReq> = {
  encode(message: ReadReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uri !== "") {
      writer.uint32(10).string(message.uri);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uri = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadReq {
    return { uri: isSet(object.uri) ? globalThis.String(object.uri) : "" };
  },

  toJSON(message: ReadReq): unknown {
    const obj: any = {};
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    return obj;
  },

  create(base?: DeepPartial<ReadReq>): ReadReq {
    return ReadReq.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReadReq>): ReadReq {
    const message = createBaseReadReq();
    message.uri = object.uri ?? "";
    return message;
  },
};

function createBaseReadRes(): ReadRes {
  return { content: "" };
}

export const ReadRes: MessageFns<ReadRes> = {
  encode(message: ReadRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content !== "") {
      writer.uint32(10).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadRes {
    return { content: isSet(object.content) ? globalThis.String(object.content) : "" };
  },

  toJSON(message: ReadRes): unknown {
    const obj: any = {};
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<ReadRes>): ReadRes {
    return ReadRes.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReadRes>): ReadRes {
    const message = createBaseReadRes();
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseWriteReq(): WriteReq {
  return { uri: "", content: "" };
}

export const WriteReq: MessageFns<WriteReq> = {
  encode(message: WriteReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uri !== "") {
      writer.uint32(10).string(message.uri);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WriteReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWriteReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uri = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WriteReq {
    return {
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: WriteReq): unknown {
    const obj: any = {};
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<WriteReq>): WriteReq {
    return WriteReq.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WriteReq>): WriteReq {
    const message = createBaseWriteReq();
    message.uri = object.uri ?? "";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseWriteRes(): WriteRes {
  return {};
}

export const WriteRes: MessageFns<WriteRes> = {
  encode(_: WriteRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WriteRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWriteRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): WriteRes {
    return {};
  },

  toJSON(_: WriteRes): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<WriteRes>): WriteRes {
    return WriteRes.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<WriteRes>): WriteRes {
    const message = createBaseWriteRes();
    return message;
  },
};

function createBasePatchReq(): PatchReq {
  return { uri: "", partial: "" };
}

export const PatchReq: MessageFns<PatchReq> = {
  encode(message: PatchReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uri !== "") {
      writer.uint32(10).string(message.uri);
    }
    if (message.partial !== "") {
      writer.uint32(18).string(message.partial);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PatchReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePatchReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uri = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.partial = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PatchReq {
    return {
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      partial: isSet(object.partial) ? globalThis.String(object.partial) : "",
    };
  },

  toJSON(message: PatchReq): unknown {
    const obj: any = {};
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.partial !== "") {
      obj.partial = message.partial;
    }
    return obj;
  },

  create(base?: DeepPartial<PatchReq>): PatchReq {
    return PatchReq.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PatchReq>): PatchReq {
    const message = createBasePatchReq();
    message.uri = object.uri ?? "";
    message.partial = object.partial ?? "";
    return message;
  },
};

function createBasePatchRes(): PatchRes {
  return {};
}

export const PatchRes: MessageFns<PatchRes> = {
  encode(_: PatchRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PatchRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePatchRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PatchRes {
    return {};
  },

  toJSON(_: PatchRes): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PatchRes>): PatchRes {
    return PatchRes.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PatchRes>): PatchRes {
    const message = createBasePatchRes();
    return message;
  },
};

function createBaseExecReq(): ExecReq {
  return { command: "" };
}

export const ExecReq: MessageFns<ExecReq> = {
  encode(message: ExecReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.command !== "") {
      writer.uint32(10).string(message.command);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.command = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecReq {
    return { command: isSet(object.command) ? globalThis.String(object.command) : "" };
  },

  toJSON(message: ExecReq): unknown {
    const obj: any = {};
    if (message.command !== "") {
      obj.command = message.command;
    }
    return obj;
  },

  create(base?: DeepPartial<ExecReq>): ExecReq {
    return ExecReq.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecReq>): ExecReq {
    const message = createBaseExecReq();
    message.command = object.command ?? "";
    return message;
  },
};

function createBaseExecRes(): ExecRes {
  return { stdout: "", stderr: "", exitCode: 0 };
}

export const ExecRes: MessageFns<ExecRes> = {
  encode(message: ExecRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stdout !== "") {
      writer.uint32(10).string(message.stdout);
    }
    if (message.stderr !== "") {
      writer.uint32(18).string(message.stderr);
    }
    if (message.exitCode !== 0) {
      writer.uint32(24).int32(message.exitCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stdout = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stderr = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecRes {
    return {
      stdout: isSet(object.stdout) ? globalThis.String(object.stdout) : "",
      stderr: isSet(object.stderr) ? globalThis.String(object.stderr) : "",
      exitCode: isSet(object.exitCode)
        ? globalThis.Number(object.exitCode)
        : isSet(object.exit_code)
        ? globalThis.Number(object.exit_code)
        : 0,
    };
  },

  toJSON(message: ExecRes): unknown {
    const obj: any = {};
    if (message.stdout !== "") {
      obj.stdout = message.stdout;
    }
    if (message.stderr !== "") {
      obj.stderr = message.stderr;
    }
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    return obj;
  },

  create(base?: DeepPartial<ExecRes>): ExecRes {
    return ExecRes.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecRes>): ExecRes {
    const message = createBaseExecRes();
    message.stdout = object.stdout ?? "";
    message.stderr = object.stderr ?? "";
    message.exitCode = object.exitCode ?? 0;
    return message;
  },
};

function createBaseCallReq(): CallReq {
  return { tool: "", paramsJson: "" };
}

export const CallReq: MessageFns<CallReq> = {
  encode(message: CallReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tool !== "") {
      writer.uint32(10).string(message.tool);
    }
    if (message.paramsJson !== "") {
      writer.uint32(18).string(message.paramsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tool = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.paramsJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CallReq {
    return {
      tool: isSet(object.tool) ? globalThis.String(object.tool) : "",
      paramsJson: isSet(object.paramsJson)
        ? globalThis.String(object.paramsJson)
        : isSet(object.params_json)
        ? globalThis.String(object.params_json)
        : "",
    };
  },

  toJSON(message: CallReq): unknown {
    const obj: any = {};
    if (message.tool !== "") {
      obj.tool = message.tool;
    }
    if (message.paramsJson !== "") {
      obj.paramsJson = message.paramsJson;
    }
    return obj;
  },

  create(base?: DeepPartial<CallReq>): CallReq {
    return CallReq.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CallReq>): CallReq {
    const message = createBaseCallReq();
    message.tool = object.tool ?? "";
    message.paramsJson = object.paramsJson ?? "";
    return message;
  },
};

function createBaseCallRes(): CallRes {
  return { resultJson: "" };
}

export const CallRes: MessageFns<CallRes> = {
  encode(message: CallRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resultJson !== "") {
      writer.uint32(10).string(message.resultJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resultJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CallRes {
    return {
      resultJson: isSet(object.resultJson)
        ? globalThis.String(object.resultJson)
        : isSet(object.result_json)
        ? globalThis.String(object.result_json)
        : "",
    };
  },

  toJSON(message: CallRes): unknown {
    const obj: any = {};
    if (message.resultJson !== "") {
      obj.resultJson = message.resultJson;
    }
    return obj;
  },

  create(base?: DeepPartial<CallRes>): CallRes {
    return CallRes.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CallRes>): CallRes {
    const message = createBaseCallRes();
    message.resultJson = object.resultJson ?? "";
    return message;
  },
};

function createBaseCompleteReq(): CompleteReq {
  return {
    summary: "",
    reply: "",
    anchor: false,
    taskLog: "",
    sleepReason: "",
    sleepRetry: false,
    resumeTaskId: "",
    anchorFacts: "",
  };
}

export const CompleteReq: MessageFns<CompleteReq> = {
  encode(message: CompleteReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.summary !== "") {
      writer.uint32(10).string(message.summary);
    }
    if (message.reply !== "") {
      writer.uint32(18).string(message.reply);
    }
    if (message.anchor !== false) {
      writer.uint32(24).bool(message.anchor);
    }
    if (message.taskLog !== "") {
      writer.uint32(34).string(message.taskLog);
    }
    if (message.sleepReason !== "") {
      writer.uint32(42).string(message.sleepReason);
    }
    if (message.sleepRetry !== false) {
      writer.uint32(48).bool(message.sleepRetry);
    }
    if (message.resumeTaskId !== "") {
      writer.uint32(58).string(message.resumeTaskId);
    }
    if (message.anchorFacts !== "") {
      writer.uint32(66).string(message.anchorFacts);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CompleteReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCompleteReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reply = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.anchor = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.taskLog = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.sleepReason = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.sleepRetry = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.resumeTaskId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.anchorFacts = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CompleteReq {
    return {
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
      reply: isSet(object.reply) ? globalThis.String(object.reply) : "",
      anchor: isSet(object.anchor) ? globalThis.Boolean(object.anchor) : false,
      taskLog: isSet(object.taskLog)
        ? globalThis.String(object.taskLog)
        : isSet(object.task_log)
        ? globalThis.String(object.task_log)
        : "",
      sleepReason: isSet(object.sleepReason)
        ? globalThis.String(object.sleepReason)
        : isSet(object.sleep_reason)
        ? globalThis.String(object.sleep_reason)
        : "",
      sleepRetry: isSet(object.sleepRetry)
        ? globalThis.Boolean(object.sleepRetry)
        : isSet(object.sleep_retry)
        ? globalThis.Boolean(object.sleep_retry)
        : false,
      resumeTaskId: isSet(object.resumeTaskId)
        ? globalThis.String(object.resumeTaskId)
        : isSet(object.resume_task_id)
        ? globalThis.String(object.resume_task_id)
        : "",
      anchorFacts: isSet(object.anchorFacts)
        ? globalThis.String(object.anchorFacts)
        : isSet(object.anchor_facts)
        ? globalThis.String(object.anchor_facts)
        : "",
    };
  },

  toJSON(message: CompleteReq): unknown {
    const obj: any = {};
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    if (message.reply !== "") {
      obj.reply = message.reply;
    }
    if (message.anchor !== false) {
      obj.anchor = message.anchor;
    }
    if (message.taskLog !== "") {
      obj.taskLog = message.taskLog;
    }
    if (message.sleepReason !== "") {
      obj.sleepReason = message.sleepReason;
    }
    if (message.sleepRetry !== false) {
      obj.sleepRetry = message.sleepRetry;
    }
    if (message.resumeTaskId !== "") {
      obj.resumeTaskId = message.resumeTaskId;
    }
    if (message.anchorFacts !== "") {
      obj.anchorFacts = message.anchorFacts;
    }
    return obj;
  },

  create(base?: DeepPartial<CompleteReq>): CompleteReq {
    return CompleteReq.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CompleteReq>): CompleteReq {
    const message = createBaseCompleteReq();
    message.summary = object.summary ?? "";
    message.reply = object.reply ?? "";
    message.anchor = object.anchor ?? false;
    message.taskLog = object.taskLog ?? "";
    message.sleepReason = object.sleepReason ?? "";
    message.sleepRetry = object.sleepRetry ?? false;
    message.resumeTaskId = object.resumeTaskId ?? "";
    message.anchorFacts = object.anchorFacts ?? "";
    return message;
  },
};

function createBaseCompleteRes(): CompleteRes {
  return { reply: "", anchorId: "" };
}

export const CompleteRes: MessageFns<CompleteRes> = {
  encode(message: CompleteRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reply !== "") {
      writer.uint32(10).string(message.reply);
    }
    if (message.anchorId !== "") {
      writer.uint32(18).string(message.anchorId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CompleteRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCompleteRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reply = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.anchorId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CompleteRes {
    return {
      reply: isSet(object.reply) ? globalThis.String(object.reply) : "",
      anchorId: isSet(object.anchorId)
        ? globalThis.String(object.anchorId)
        : isSet(object.anchor_id)
        ? globalThis.String(object.anchor_id)
        : "",
    };
  },

  toJSON(message: CompleteRes): unknown {
    const obj: any = {};
    if (message.reply !== "") {
      obj.reply = message.reply;
    }
    if (message.anchorId !== "") {
      obj.anchorId = message.anchorId;
    }
    return obj;
  },

  create(base?: DeepPartial<CompleteRes>): CompleteRes {
    return CompleteRes.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CompleteRes>): CompleteRes {
    const message = createBaseCompleteRes();
    message.reply = object.reply ?? "";
    message.anchorId = object.anchorId ?? "";
    return message;
  },
};

function createBaseHandshakeReq(): HandshakeReq {
  return { token: "" };
}

export const HandshakeReq: MessageFns<HandshakeReq> = {
  encode(message: HandshakeReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HandshakeReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHandshakeReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HandshakeReq {
    return { token: isSet(object.token) ? globalThis.String(object.token) : "" };
  },

  toJSON(message: HandshakeReq): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create(base?: DeepPartial<HandshakeReq>): HandshakeReq {
    return HandshakeReq.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HandshakeReq>): HandshakeReq {
    const message = createBaseHandshakeReq();
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseHandshakeRes(): HandshakeRes {
  return { ok: false, error: "" };
}

export const HandshakeRes: MessageFns<HandshakeRes> = {
  encode(message: HandshakeRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    if (message.error !== "") {
      writer.uint32(18).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HandshakeRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHandshakeRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HandshakeRes {
    return {
      ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false,
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: HandshakeRes): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<HandshakeRes>): HandshakeRes {
    return HandshakeRes.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HandshakeRes>): HandshakeRes {
    const message = createBaseHandshakeRes();
    message.ok = object.ok ?? false;
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseRegisterTokenReq(): RegisterTokenReq {
  return { token: "", taskId: "", role: "" };
}

export const RegisterTokenReq: MessageFns<RegisterTokenReq> = {
  encode(message: RegisterTokenReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    if (message.taskId !== "") {
      writer.uint32(18).string(message.taskId);
    }
    if (message.role !== "") {
      writer.uint32(26).string(message.role);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterTokenReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterTokenReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.role = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterTokenReq {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      role: isSet(object.role) ? globalThis.String(object.role) : "",
    };
  },

  toJSON(message: RegisterTokenReq): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.role !== "") {
      obj.role = message.role;
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterTokenReq>): RegisterTokenReq {
    return RegisterTokenReq.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterTokenReq>): RegisterTokenReq {
    const message = createBaseRegisterTokenReq();
    message.token = object.token ?? "";
    message.taskId = object.taskId ?? "";
    message.role = object.role ?? "";
    return message;
  },
};

function createBaseRegisterTokenRes(): RegisterTokenRes {
  return {};
}

export const RegisterTokenRes: MessageFns<RegisterTokenRes> = {
  encode(_: RegisterTokenRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterTokenRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterTokenRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RegisterTokenRes {
    return {};
  },

  toJSON(_: RegisterTokenRes): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<RegisterTokenRes>): RegisterTokenRes {
    return RegisterTokenRes.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RegisterTokenRes>): RegisterTokenRes {
    const message = createBaseRegisterTokenRes();
    return message;
  },
};

function createBaseRevokeTokenReq(): RevokeTokenReq {
  return { token: "" };
}

export const RevokeTokenReq: MessageFns<RevokeTokenReq> = {
  encode(message: RevokeTokenReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeTokenReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeTokenReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevokeTokenReq {
    return { token: isSet(object.token) ? globalThis.String(object.token) : "" };
  },

  toJSON(message: RevokeTokenReq): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create(base?: DeepPartial<RevokeTokenReq>): RevokeTokenReq {
    return RevokeTokenReq.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RevokeTokenReq>): RevokeTokenReq {
    const message = createBaseRevokeTokenReq();
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseRevokeTokenRes(): RevokeTokenRes {
  return {};
}

export const RevokeTokenRes: MessageFns<RevokeTokenRes> = {
  encode(_: RevokeTokenRes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeTokenRes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeTokenRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RevokeTokenRes {
    return {};
  },

  toJSON(_: RevokeTokenRes): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<RevokeTokenRes>): RevokeTokenRes {
    return RevokeTokenRes.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RevokeTokenRes>): RevokeTokenRes {
    const message = createBaseRevokeTokenRes();
    return message;
  },
};

export type LogosDefinition = typeof LogosDefinition;
export const LogosDefinition = {
  name: "Logos",
  fullName: "logos.kernel.v1.Logos",
  methods: {
    /** Agent primitives */
    read: {
      name: "Read",
      requestType: ReadReq,
      requestStream: false,
      responseType: ReadRes,
      responseStream: false,
      options: {},
    },
    write: {
      name: "Write",
      requestType: WriteReq,
      requestStream: false,
      responseType: WriteRes,
      responseStream: false,
      options: {},
    },
    patch: {
      name: "Patch",
      requestType: PatchReq,
      requestStream: false,
      responseType: PatchRes,
      responseStream: false,
      options: {},
    },
    exec: {
      name: "Exec",
      requestType: ExecReq,
      requestStream: false,
      responseType: ExecRes,
      responseStream: false,
      options: {},
    },
    call: {
      name: "Call",
      requestType: CallReq,
      requestStream: false,
      responseType: CallRes,
      responseStream: false,
      options: {},
    },
    complete: {
      name: "Complete",
      requestType: CompleteReq,
      requestStream: false,
      responseType: CompleteRes,
      responseStream: false,
      options: {},
    },
    /** Kernel management interface (runtime-facing, not agent-facing) */
    handshake: {
      name: "Handshake",
      requestType: HandshakeReq,
      requestStream: false,
      responseType: HandshakeRes,
      responseStream: false,
      options: {},
    },
    registerToken: {
      name: "RegisterToken",
      requestType: RegisterTokenReq,
      requestStream: false,
      responseType: RegisterTokenRes,
      responseStream: false,
      options: {},
    },
    revokeToken: {
      name: "RevokeToken",
      requestType: RevokeTokenReq,
      requestStream: false,
      responseType: RevokeTokenRes,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface LogosServiceImplementation<CallContextExt = {}> {
  /** Agent primitives */
  read(request: ReadReq, context: CallContext & CallContextExt): Promise<DeepPartial<ReadRes>>;
  write(request: WriteReq, context: CallContext & CallContextExt): Promise<DeepPartial<WriteRes>>;
  patch(request: PatchReq, context: CallContext & CallContextExt): Promise<DeepPartial<PatchRes>>;
  exec(request: ExecReq, context: CallContext & CallContextExt): Promise<DeepPartial<ExecRes>>;
  call(request: CallReq, context: CallContext & CallContextExt): Promise<DeepPartial<CallRes>>;
  complete(request: CompleteReq, context: CallContext & CallContextExt): Promise<DeepPartial<CompleteRes>>;
  /** Kernel management interface (runtime-facing, not agent-facing) */
  handshake(request: HandshakeReq, context: CallContext & CallContextExt): Promise<DeepPartial<HandshakeRes>>;
  registerToken(
    request: RegisterTokenReq,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<RegisterTokenRes>>;
  revokeToken(request: RevokeTokenReq, context: CallContext & CallContextExt): Promise<DeepPartial<RevokeTokenRes>>;
}

export interface LogosClient<CallOptionsExt = {}> {
  /** Agent primitives */
  read(request: DeepPartial<ReadReq>, options?: CallOptions & CallOptionsExt): Promise<ReadRes>;
  write(request: DeepPartial<WriteReq>, options?: CallOptions & CallOptionsExt): Promise<WriteRes>;
  patch(request: DeepPartial<PatchReq>, options?: CallOptions & CallOptionsExt): Promise<PatchRes>;
  exec(request: DeepPartial<ExecReq>, options?: CallOptions & CallOptionsExt): Promise<ExecRes>;
  call(request: DeepPartial<CallReq>, options?: CallOptions & CallOptionsExt): Promise<CallRes>;
  complete(request: DeepPartial<CompleteReq>, options?: CallOptions & CallOptionsExt): Promise<CompleteRes>;
  /** Kernel management interface (runtime-facing, not agent-facing) */
  handshake(request: DeepPartial<HandshakeReq>, options?: CallOptions & CallOptionsExt): Promise<HandshakeRes>;
  registerToken(
    request: DeepPartial<RegisterTokenReq>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<RegisterTokenRes>;
  revokeToken(request: DeepPartial<RevokeTokenReq>, options?: CallOptions & CallOptionsExt): Promise<RevokeTokenRes>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
